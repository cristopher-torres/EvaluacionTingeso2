server:
  port: 0

spring:
  application:
    name: M6
  datasource:
    url: jdbc:mysql://localhost:3306/m6?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: ADMIN
    password: cris123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
