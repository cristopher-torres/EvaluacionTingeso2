server:
  port: 0

spring:
  application:
    name: M2
  datasource:
    url: jdbc:mysql://localhost:3306/m2?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: ADMIN
    password: cris123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}